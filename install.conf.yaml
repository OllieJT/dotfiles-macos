- clean: ["~"]

- link:
      ~/.gitconfig:
      ~/.zshrc:
      ~/.zprofile:
      ~/.config/gh:
          create: true
          path: config/gh
      ~/.config/git:
          create: true
          path: config/git
- shell:
      - command: ./setup_homebrew.zsh
        stdout: true
        srderr: true

- clean: ["~"]

- link:
    ~/.zshrc: zshrc
      ~/.gitconfig:
      ~/.config/gh:
          create: true
          path: config/gh
      ~/.config/git:
          create: true
          path: config/git

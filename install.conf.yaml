- clean: ["~"]

- link:
      ~/.gitconfig:
      ~/.zshrc:
      ~/.zprofile:
      ~/.config/gh:
          create: true
          path: config/gh
      ~/.config/git:
          create: true
          path: config/git
